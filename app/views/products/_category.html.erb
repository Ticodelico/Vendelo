<li class="mr-2">
  <%= link_to products_path(category_id: category.id), class: 'bg-transparent hover:bg-gradient-to-r from-indigo-500 via-purple-500 to-pink-500 text-white text-lg font-semibold hover:text-white py-2 px-4 hover:border-transparent rounded' do %>
    <%= category.name %>
  <% end %>
</li>
